---
import { type Lang } from "../../i18n";

interface Props {
  lang: Lang;
  t: {
    what: string;
    why: string;
    how: string;
    partners: string;
  };
}

const { lang, t } = Astro.props;
---

<nav
  class="sticky top-0 z-50 bg-white border-b-4 border-accent shadow-sm"
>
  <div class="mx-auto px-4 flex items-center py-4 flex-wrap gap-3">
    <a
      href={`/${lang}/`}
      class="flex items-center no-underline hover:no-underline"
    >
      <img
        src="/dual-ai-navbar.png"
        alt="DUAL.AI.TEACHer"
        class="h-10 sm:h-12 ml-6 w-auto"
      />
    </a>
    <ul class="hidden md:flex gap-8 list-none m-0 p-0 ml-16 flex-wrap">
      <li>
        <a
          href="#about"
          class="text-base font-bold text-slate-600 hover:text-brand no-underline transition-colors uppercase tracking-wide"
         >{t.what}</a>
      </li>
      <li>
        <a
          href="#why"
          class="text-base font-bold text-slate-600 hover:text-brand no-underline transition-colors uppercase tracking-wide"
         >{t.why}</a>
      </li>
      <li>
        <a
          href="#how"
          class="text-base font-bold text-slate-600 hover:text-brand no-underline transition-colors uppercase tracking-wide"
         >{t.how}</a>
      </li>
      <li>
        <a
          href="#about-partners"
          class="text-base font-bold text-slate-600 hover:text-brand no-underline transition-colors uppercase tracking-wide"
         >{t.partners}</a>
      </li>
    </ul>
    <button
      id="mobileMenuBtn"
      class="md:hidden ml-auto p-2 rounded-lg hover:bg-surface-2 transition-colors cursor-pointer border-0 bg-transparent text-brand"
    >
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"
        ><path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2.5"
          d="M4 6h16M4 12h16M4 18h16"></path></svg
      >
    </button>
  </div>

  <div
    id="mobileMenu"
    class="hidden md:hidden border-t border-surface-3 bg-white px-6 pb-6 shadow-xl"
  >
    <ul class="list-none m-0 p-0 flex flex-col gap-2 pt-4">
      <li>
        <a
          href="#about"
          class="block py-3 text-lg font-black text-brand uppercase border-b border-slate-100"
         >{t.what}</a>
      </li>
      <li>
        <a
          href="#why"
          class="block py-3 text-lg font-black text-brand uppercase border-b border-slate-100"
         >{t.why}</a>
      </li>
      <li>
        <a
          href="#how"
          class="block py-3 text-lg font-black text-brand uppercase border-b border-slate-100"
         >{t.how}</a>
      </li>
      <li>
        <a
          href="#about-partners"
          class="block py-3 text-lg font-black text-brand uppercase border-b border-slate-100"
         >{t.partners}</a>
      </li>
    </ul>
  </div>
</nav>
